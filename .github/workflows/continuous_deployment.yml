
on:
  push:
    branches:
      - release
  pull_request:
    branches:
      - release
  workflow_dispatch:

#needs: continuous_delivery.
# ssh ind i vm

# SCP docker-compose.server.yml

# docker-compose
  # pull
  # up
