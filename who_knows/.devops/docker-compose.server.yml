services:
  whoknows_app:
    image: ghcr.io/martin-s-angels/whoknows_variations_server:latest
    restart: always
    ports:
      - "8080:8080"
    networks:
      - my_network
    volumes:
    - /home/adminuser/env/.env:/who_knows/.dotenv/.env:ro #.env (defined in gh actions) 
    - /home/adminuser/db/whoknows.db:/who_knows/db/whoknows.db #db
    depends_on:
    - database

  whoknows_nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
      - "443:443" #https certificate
    depends_on:
      - whoknows_app
    networks:
      - my_network
    volumes:
      - ./network/nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro

  database:
    image: ghcr.io/martin-s-angels/whoknows_variations_database:latest
    restart: always
    container_name: whoknows_database
    ports:
      - "5432:5432"
    env_file:
      - env/.env
    volumes: 
      - database:/var/lib/postgresql
    networks:
      - my_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
volumes:
  database:


networks:
  my_network:
    driver: bridge
